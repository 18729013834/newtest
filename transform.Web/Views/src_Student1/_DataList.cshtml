@using MetaShare.Common.Core.Presentation
@using transform.Entities
@using transform.Web.HtmlExtensions
@using transform.Entities.src
@using transform.Web.Models
<!--add customized code between this region-->
<!--add customized code between this region-->

@model IndexViewModel<Student1>
        <table class="table table-striped table-bordered table-hover">
            <thead>
            <tr>
		<th class="text-left">Name</th>
		<th class="text-left">StudentName</th>
		<th class="text-right">StudentNUM</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            @if (Model.TargetPager != null && Model.TargetPager.Datas != null)
            {
            foreach (transform.Entities.src.Student1 student1Item in Model.TargetPager.Datas)
                        {
                <tr>
			<td  class="text-left">
				@Html.DisplayFor(modelItem => student1Item.Name)
			</td>
			<td class="text-left">
				@Html.DisplayFor(modelItem => student1Item.StudentName)
			</td>
			<td class="text-right">
				@Html.DisplayFor(modelItem => student1Item.StudentNUM)
			</td>
                    <td>
                        @Html.ActionLink(" ", "Details", new { id = student1Item.Id, Controller = "Student1", area = "src"}, new {@class="m_details", title="Details" })
                        @Html.ActionLink(" ", "Edit", new { id = student1Item.Id, Controller = "Student1", area = "src"}, new {@class="m_edit", title="Edit" })
                        @Html.ActionLink(" ", "#", "#", new {href = "javascript:void(0);",@class="m_delete", title="Delete", onclick = " javascript:metaShare.basic.prototype.deleteTableColumn(this,'" + student1Item.Id + "','" + Url.Action("Delete",new {Controller = "Student1", area = "src"}) + "'); return false; "})
                    </td>
                </tr>
            }
            }
<!--add customized code between this region-->
<!--add customized code between this region-->
            </tbody>
        </table>


        <div class="UserPagerDiv text-center">
    @{
        object conditions = new
        {
            Name = Model.SearchModel != null ? Model.SearchModel.Name : "",
        };
    }
        @Ajax.AjaxRenderPager(Model.TargetPager, "Search", "Student1","Student1_DataList", conditions)
        </div>
