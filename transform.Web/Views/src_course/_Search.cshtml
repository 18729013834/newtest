@using transform.Web.HtmlExtensions
@using transform.Web.Models
<!--add customized code between this region-->
<!--add customized code between this region-->

@model SearchModel
<div class="search-container">
    @using (Html.BeginForm("Search", "course", FormMethod.Post, new {data_ajax = "true", data_ajax_method = "POST", data_ajax_mode = "replace", data_ajax_update = "#course_DataList", name = "course_Search", id = "course_Search"}))
    {
        @Html.AntiForgeryToken()
        @Html.TextBoxFor(m => m.Name, new {@class = "form-control", placeholder ="Name"})
        @Html.HiddenFor(m => m.Pager.PageSize)

        <input class="m_btn" type="submit" value="Search"/>
    }
</div>

